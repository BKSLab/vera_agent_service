[pytest]
asyncio_mode = auto
markers =
    integration: тесты на реальной инфраструктуре (RabbitMQ/Redis из docker-compose.yml), не только на моках
    external: опциональные contract-тесты внешних API; требуют явного RUN_POLZA_CONTRACT_TESTS=1
