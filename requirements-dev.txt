-r requirements.txt

# Только для мок MCP-сервера в тестах (tests/fixtures/mock_mcp_server.py,
# Этап 3) — реальный сервис запускается через hypercorn (entrypoint.sh).
uvicorn==0.51.0

pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
testcontainers==4.14.2
ruff==0.15.20
